{
  "name": "peer-server",
  "version": "1.0.0",
  "description": "server peer to peer",
  "main": "index.js",
  "scripts": {
    "dev": "set PORT=9000 npm start",
    "start": "npx peerjs --port $PORT --key peerjs --path /",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Aline Soares da Silva",
  "license": "ISC",
  "dependencies": {
    "peer": "^0.6.1"
  }
}
